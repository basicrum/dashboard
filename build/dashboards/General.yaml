apiVersion: 1

providers:
  - name: Basic RUM  # A uniquely identifiable name for the provider
    folder: Custom   # The folder where to place the dashboards
    type: file
    options:
      path:  /etc/grafana/provisioning/dashboards/General.json