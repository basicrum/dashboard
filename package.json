{
  "name": "basicrum-dashboard",
  "version": "1.0.0",
  "description": "Build scripts and source files used to build customized Grafana Docker image.",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && jest"
  },
  "jest": {
    "testPathIgnorePatterns": ["basicrum-api-datasource"]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "deepmerge": "^4.3.1",
    "glob": "^8.0.3",
    "jest": "^29.7.0"
  }
}
